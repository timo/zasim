VERSION = '0.0.2'

from setuptools import setup

setup(
      name = 'zasim',
      version = VERSION,
      author = 'zasim team',
      description = 'A cellular automaton simulation environment.',
      keywords = '',
      url = '',
      packages = ["zasim"],
    )
